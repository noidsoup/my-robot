# AI Runbook

Operational notes for humans and agents. Keep this short and current.
No secrets — point at `.env.example` for required env vars.

## Local

```bash
# Prefer clone + dry-run before apply:
#   path/to/my-robot/bootstrap.sh --dry-run .
#   path/to/my-robot/bootstrap.sh .
bash .verify.sh
```

## Deploy / release

_(fill in: host, branch, how to ship)_

## Incidents

_(fill in: who to ping, how to roll back)_
