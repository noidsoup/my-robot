Rolling high-level summary. Two sections only — overwrite, don't append.
Detailed dated history lives in `AI_SESSION_MEMORY.md`.

## Current session
Idle. Template is feature-complete and presentation-ready.

## Previous session
Added the L4 Knowledge Wiki layer (Obsidian vault at `<repo> wiki/`, SCHEMA/index/log templates, llm-wiki rule, indexer discovers `* wiki/` vaults) — conventions ported from caper-app. Rewrote both READMEs in plain English. Everything pushed, verified idempotent on Node + Python smoke repos. Both repos public and prospect-ready. Optional next steps: test the curl standalone path, demo GIF, portfolio blurb.
